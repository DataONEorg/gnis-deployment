---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: gnis
  name: gnis-pvc
spec:
  storageClassName: "manual"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 75Gi
